{
  "name": "ptokens-constants",
  "version": "0.4.0",
  "description": "Group all the shared constants for the pTokens libraries'",
  "engines": {
    "npm": ">=8.0.0 <10.0.0",
    "node": ">=16.15.0 <17.0.0"
  },
  "scripts": {
    "lint": "eslint .",
    "clean": "rm -rf $(pwd)/build",
    "docker-build": "PROJECT_ROOT=$(pwd) ./docker-build.sh"
  },
  "author": "oliviera9",
  "license": "MIT"
}
