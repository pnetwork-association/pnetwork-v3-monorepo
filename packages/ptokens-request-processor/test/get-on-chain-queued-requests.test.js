const assert = require('assert')

describe('Get on chain queued requests module tests', () => {
  describe('getQueuedRequests', () => {
    it('Should get the requests from the queue correctly', () => {
      assert.equal('hell', 'hell')
    })
  })
})
